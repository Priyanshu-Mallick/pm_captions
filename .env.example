# Sentry crash/error reporting DSN.
# Get this from your Sentry project: Settings → Projects → <project> → Client Keys (DSN).
# Leave empty to disable error reporting (e.g. local development).
SENTRY_DSN=
